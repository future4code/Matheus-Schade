export type user = {
   id: string
   name: string
   nickname: string
   email: string
   address: string
}

export type Adress = {
   address: string
   neighborhood: string
   city: string
   state: string
}